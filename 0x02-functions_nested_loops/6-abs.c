#include "main.h"
/**
 * _abs - main function
 *
 * @n: integer to make absolute or the integer to be computed
 * Return:the  absolute value of the integer
 *
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
